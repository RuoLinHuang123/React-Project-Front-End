import { Text } from "@chakra-ui/react";
import { Link } from "react-router-dom";

const NavTitle = () => {
  return <Text><Link to={'/'}>Solar System Database</Link></Text>;
};

export default NavTitle;
